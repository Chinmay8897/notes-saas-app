# Production environment variables for Netlify
# Set these in your Netlify dashboard under Site settings > Environment variables

# Your backend API URL (deploy your backend to Heroku, Railway, or Vercel)
VITE_API_URL=https://your-backend-url.com/api

# Note: Don't put sensitive backend variables here as they will be exposed in the client bundle
# Only VITE_ prefixed variables are available in the frontend build