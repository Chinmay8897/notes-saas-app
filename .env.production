# Production environment variables for Netlify (Frontend only)
# Set these in your Netlify dashboard under Site settings > Environment variables

# Your backend API URL (deploy your backend separately)
VITE_API_URL=https://your-backend-api.herokuapp.com/api

# Note: Only VITE_ prefixed variables are included in the frontend build
# Backend secrets (MONGODB_URI, JWT_SECRET) should NEVER be in frontend environment files